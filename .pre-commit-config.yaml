repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.8.2
    hooks:
      - id: ruff
        types_or: [python, pyi, jupyter]
        args: [--extend-select, I, --fix]
      - id: ruff-format
        types_or: [python, pyi, jupyter]
